package Tree.Trie;

public class Trie {
    /**
     * Trie 前缀树或称字典树，是一棵有根树，其🇺每个节点包含以下字段：
     *      1。 指向子节点的指针数组，children。
     *      2。 boolean isEnd， 表示该节点是否为字符串的结尾
     * 前缀树又称单词查找树，是一种树形的结构，用于存储大量的字符串。
     * 优点： 利用字符串的公共前缀来节约存储空间
     * Trie树主要利用词的公共前缀缩小查词范围，通过状态间的映射关系避免了字符的遍历，从而达到高效检索的目的
     *
     * **/
}
